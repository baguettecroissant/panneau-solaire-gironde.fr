# Même configuration que `.dev.vars.example`.
# À définir comme secrets chiffrés dans Cloudflare Pages.
VUD_API_KEY=
# Renseigner la clé publishable Supabase actuelle ou, à défaut, l’ancienne clé anon JWT.
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_ANON_KEY=
